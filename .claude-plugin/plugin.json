{
  "name": "joystick",
  "version": "0.1.0",
  "description": "Renders a Claude Code session as a readable, narrated timeline in a local browser panel.",
  "author": { "name": "deva" },
  "keywords": ["observability", "timeline", "visualization"],
  "hooks": "./hooks/hooks.json",
  "userConfig": {
    "port": {
      "type": "number",
      "title": "Sidecar port",
      "description": "Local port the joystick sidecar listens on (127.0.0.1 only).",
      "default": 8787,
      "min": 1024,
      "max": 65535
    }
  }
}
